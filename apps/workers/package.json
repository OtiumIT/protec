{
  "name": "workers",
  "version": "1.0.0",
  "private": true,
  "description": "Python workers para processamento pesado",
  "scripts": {
    "dev": "python3 src/main.py --loop",
    "run-once": "python3 src/main.py",
    "build": "echo 'Python não requer build'",
    "lint": "ruff check .",
    "type-check": "mypy .",
    "clean": "find . -type d -name __pycache__ -exec rm -r {} +"
  }
}
