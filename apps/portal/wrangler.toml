# Cloudflare Pages – frontend (portal)
# Deploy: na raiz do monorepo: pnpm run deploy:portal (usa projeto "iatax" ou CLOUDFLARE_PAGES_PROJECT)
# Ou conecte o repositório no dashboard (ver docs/CLOUDFLARE_PAGES.md).

name = "portal"
compatibility_date = "2024-01-01"
pages_build_output_dir = "dist"
